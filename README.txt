
How to Run the V20 Web App (Mobile-Friendly)

1. Install dependencies:
   pip install -r requirements.txt

2. Run the app:
   python app.py

3. On PC: open http://localhost:5000
   On phone (same Wi-Fi): open http://<your-PC-IP>:5000

Note: This version uses mocked stock data for offline/local testing.
